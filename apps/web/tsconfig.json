{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2021" /* Set a more recent JavaScript version that supports modern features. */,
    "lib": [
      "ES2021",
      "DOM",
      "DOM.Iterable"
    ] /* Include relevant library files for React and modern JavaScript features. */,
    "jsx": "react-jsx" /* Use the new JSX transform in React 17+. */,

    /* Modules */
    "module": "ESNext" /* Use ESNext module system which is more aligned with modern JavaScript and bundlers. */,
    "moduleResolution": "Bundler" /* Use Node.js module resolution algorithm. */,
    "baseUrl": "./src" /* Set the base directory to resolve non-relative module names. */,
    "paths": {
      "@/*": ["*"]
    },
    "resolveJsonModule": true /* Enable importing .json files. */,
    "allowImportingTsExtensions": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Emit */
    "sourceMap": false /* Create source map files for emitted JavaScript files, useful for debugging. */,
    "outDir": "./build" /* Specify an output folder for all emitted files. */,
    "removeComments": true /* Remove comments from the emitted JavaScript files. */,

    /* Interop Constraints */
    "esModuleInterop": true /* Enable support for CommonJS modules with a default export. */,
    "allowSyntheticDefaultImports": true /* Allow default imports from modules with no default export. */,

    /* Type Checking */
    "strict": true /* Enable all strict type-checking options. */,
    "noImplicitAny": true /* Enable error reporting for expressions and declarations with an implied 'any' type. */,
    "strictNullChecks": true /* When type checking, take into account 'null' and 'undefined'. */,
    "strictFunctionTypes": true /* Ensure function types are strictly checked. */,
    "strictBindCallApply": true /* Strictly check `bind`, `call`, and `apply` arguments. */,
    "strictPropertyInitialization": true /* Check for class properties that are declared but not set in the constructor. */,
    "noImplicitThis": true /* Enable error reporting when `this` is given the type `any`. */,
    "alwaysStrict": true /* Ensure `use strict` is always emitted. */,
    "noUnusedLocals": true /* Enable error reporting when local variables aren't read. */,
    "noUnusedParameters": true /* Raise an error when a function parameter isn't read. */,
    "noImplicitReturns": true /* Enable error reporting for codepaths that do not explicitly return in a function. */,
    "noFallthroughCasesInSwitch": true /* Enable error reporting for fallthrough cases in switch statements. */,
    "forceConsistentCasingInFileNames": true /* Ensure that casing is consistent in file names. */,
    "isolatedModules": true /* Ensure that each file is a separate module. */,
    "useDefineForClassFields": true /* Emit ECMAScript-standard-compliant class fields. */,
    "verbatimModuleSyntax": true /* Ensure imports are only used for their side-effects. */,
    "allowJs": false /* Allow JavaScript files to be imported. */,
    "noUncheckedSideEffectImports": true /* Ensure that side-effect imports are not unchecked. */,

    /* Completeness */
    "skipLibCheck": true /* Skip type checking all .d.ts files for faster compilation. */,

    "types": ["node"]
  },
  "include": [
    "src/**/*"
  ] /* Include all TypeScript files in the src directory. */,
  "exclude": [
    "node_modules",
    "build"
  ] /* Exclude the node_modules and build directories. */
}
